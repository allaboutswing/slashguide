# 저장소 기여 가이드

## 프로젝트 구조 및 모듈 구성

이 저장소는 별도 의존성 없이 실행되는 Codex 슬래시 명령어 한국어 튜토리얼입니다.

- `index.html`: 페이지 구조, 메타데이터, 탐색 메뉴, 문서 링크를 정의합니다.
- `styles.css`: 반응형 레이아웃, 색상, 타이포그래피, 상호작용 상태를 관리합니다.
- `app.js`: 명령어 데이터, 필터, 검색, 복사 기능, 로컬 학습 기록을 관리합니다.
- `README.md`: 프로젝트 개요, 기능 목록, 공식 출처를 설명합니다.
- `prompt.md`: 최초 제품 요구사항이며 작업 범위를 판단하는 기준으로 사용합니다.

현재 생성 파일, 에셋 디렉터리, 자동화 테스트는 없습니다. 정적 에셋을 추가할 경우 `assets/` 디렉터리에 보관하세요.

## 빌드, 테스트 및 개발 명령어

패키지 설치나 빌드 과정은 필요하지 않습니다. 빠르게 확인하려면 `index.html`을 직접 열고, 로컬 서버가 필요하면 다음 명령을 실행하세요.

```powershell
python -m http.server 8000
```

이후 `http://localhost:8000`에 접속합니다. 변경 사항을 제출하기 전에 JavaScript 문법을 검사하세요.

```powershell
node --check app.js
```

## 코딩 스타일 및 명명 규칙

HTML, CSS, JavaScript에는 공백 두 칸 들여쓰기를 사용합니다. 시맨틱 HTML과 브라우저 기본 API를 우선하며, 현재 기술만으로 간단히 구현할 수 있는 기능에 프레임워크나 의존성을 추가하지 마세요. CSS 클래스와 HTML ID에는 `kebab-case`, JavaScript 변수와 함수에는 `camelCase`를 사용합니다. 사용자에게 보이는 문구는 간결한 한국어로 작성하세요. 명령어 동작이 바뀌면 OpenAI 공식 문서 링크도 추가하거나 갱신합니다.

컨트롤 레이블, 키보드 포커스 스타일, `aria-pressed` 상태, 동작 줄이기 설정, 모바일 레이아웃 등 기존 접근성 기능을 유지하세요.

## 테스트 지침

현재 테스트 프레임워크나 커버리지 기준은 없습니다. 변경할 때마다 `node --check app.js`를 실행하고 데스크톱 및 모바일 너비에서 페이지를 직접 확인하세요. 환경과 단계 필터의 조합, 대소문자를 구분하지 않는 검색, 검색 결과 없음 상태, 복사 기능, 학습 완료 전환, 새로고침 후 기록 유지, 키보드 탐색, `/` 검색 단축키를 점검합니다. 애플리케이션 로직이 단순한 DOM 동작 범위를 넘어가면 자동화 테스트를 추가하세요.

## 커밋 및 풀 리퀘스트 지침

커밋 메시지는 쉽고 간략한 한국어로 작성하세요. 제목은 `검색 단축키 추가` 또는 `CLI 명령어 정보 갱신`처럼 변경 내용을 바로 이해할 수 있는 짧은 명령형으로 작성합니다.

풀 리퀘스트에는 사용자가 체감하는 변경 사항과 수행한 검증을 설명하고 관련 이슈를 연결하세요. 레이아웃이나 스타일을 변경했다면 전후 스크린샷을 첨부합니다. 명령어 지원 환경이나 공식 출처 URL이 달라졌다면 리뷰어가 최신 문서와 대조할 수 있도록 명시하세요.
